"use client";

import PropertySearch from "./PropertySearch";

export default function ComprarFilters() {
  return (
    <PropertySearch
      showPurpose={false}
      defaultPurpose="Venda"
    />
  );
}