export type Property = {
  code: string;
  title: string;
  location: string;
  price: string;
  image: string;
  tag: string;
  area: string;
  bedrooms: string;
  parking: string;
};

export type Neighborhood = {
  name: string;
  slug: string;
  image: string;
  description: string;
};

export type Testimonial = {
  quote: string;
  name: string;
  profile: string;
};